My3A := function(p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

F<a, a1, a2, a3> := FreeGroup (4);
Alpha := [a, a1, a2];
Beta := [a3];

// common relations, typically commutators
Comms := [
(a1, a) = a2, (a2, a)= a3, (a1, a2) = 1,
a2^p = 1, a3^p = 1] cat
&cat([[(Beta[i], Alpha[j]) = Id(F) : j
 in [1..3]]: i in [1]]);

L := [];

// (311)a

G := quo <F | Comms, a^(p^2) = a3, a1^p = 1>;
Append (~L, G);

// (311)b_r

for r in [1, nu] do
  G := quo <F | Comms, a^p = 1, a1^(p^2) = a3^r>;
  Append (~L, G);
end for;

// (221)a

G := quo <F | Comms, a^p = a3, a1^(p^2) = 1>;
Append (~L, G);

// (221)b_r

for r in [1, nu] do
  G := quo <F | Comms, a^(p^2) = 1, a1^p = a3^r>;
  Append (~L, G);
end for;

// (2111)d

G := quo <F | Comms, a^(p^2) = 1, a1^p = 1>;
Append (~L, G);

// (2111)e

G := quo <F | Comms, a^p = 1, a1^(p^2) = 1>;
Append (~L, G);


return L;
end function;

My3B := function(p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

F<a, a1, a2, a3, ga> := FreeGroup (5);
Alpha := [a, a1, a2];
Beta := [a3, ga];

// common relations, typically commutators
Comms := [
(a1, a) = a2, (a2, a)= a3, (a1, a2) = 1, (a3, ga) = 1,
a2^p = 1, a3^p = 1] cat
&cat([[(Beta[i], Alpha[j]) = Id(F) : j
 in [1..3]]: i in [1..2]]);

L := [];

// (2111)a

G := quo <F | Comms, a^(p) = a3, a1^p = 1, ga^p = 1>;
Append (~L, G);

// (2111)b_r

for r in [1, nu] do
  G := quo <F | Comms, a^p = 1, a1^(p) = a3^r, ga^p = 1>;
  Append (~L, G);
end for;

// (1^5)

G := quo <F | Comms, a^p = 1, a1^(p) = 1, ga^p = 1>;
Append (~L, G);

// (2111)c

G := quo <F | Comms, a^p = 1, a1^(p) = 1, ga^p = a3>;
Append (~L, G);

return L;
end function;


My3 := function(p)
return My3A(p) cat My3B(p);
end function;

My4 := function(p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

PairPhi4 := function(r, p)
  for x in GF(p) do

      if 4*x eq (w^(2*r + 1) - 1) mod p and x ne 0 then
        return Z!x;
      end if;
    
  end for;
end function;

F<a, a1, a2, b1, b2> := FreeGroup (5);
Alpha := [a, a1, a2];
Beta := [b1, b2];

// common relations, typically commutators
Comms := [
(a1, a) = b1, (a2, a)= b2, (a1, a2) = 1, (b1, b2) = 1,
b1^p = 1, b2^p = 1] cat
&cat([[(Beta[i], Alpha[j]) = Id(F) : j
 in [1..3]]: i in [1..2]]);

L := [];


// (221)a

G := quo <F | Comms, a^p = b2, a1^p = b1,
 a2^p = 1>;
Append (~L, G);

// (221)b

G := quo <F | Comms, a^p = b2, a1^p = 1,
 a2^p = b1>;
Append (~L, G);

// (221)c

G := quo <F | Comms, a^p = 1, a1^p = b1,
 a2^p = b2>;
Append (~L, G);

// (221)d_r

for r in [1..(p-1) div 2] do
  G := quo <F | Comms, a^p = 1, a1^p = b1^(w^r mod p),
  a2^p = b2>;
  Append (~L, G);
end for;

// (221)e

G := quo <F | Comms, a^p = 1, a1^(4*p) = b2^(-1),
 a2^p = b1*b2>;
Append (~L, G);

// (221)f_0

G := quo <F | Comms, a^p = 1, a1^p = b2,
 a2^p = b1^nu>;
Append (~L, G);

// (221)f_r

for r in [1..(p-1) div 2] do
  x := PairPhi4 (r, p);
  G := quo <F | Comms, a^p = 1, a1^p = b2^x,
  a2^p = b1*b2>;
  Append (~L, G);
end for;

// (2111)a

G := quo <F | Comms, a^p = b2, a1^p = 1,
 a2^p = 1>;
Append (~L, G);

// (2111)b

G := quo <F | Comms, a^p = 1, a1^p = b1,
 a2^p = 1>;
Append (~L, G);

// (2111)c

G := quo <F | Comms, a^p = 1, a1^p = 1,
 a2^p = b1>;
Append (~L, G);

// (1^5)

G := quo <F | Comms, a^p = 1, a1^p = 1,
 a2^p = 1>;
Append (~L, G);

return L;
end function;

My6 := function(p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

PairPhi6 := function(r, p)
  for x in GF(p) do

      if 4*x eq (w^(2*r + 1) - 1) mod p and x ne 0 then
        return Z!x;
      end if;
    
  end for;
end function;

F<a1, a2, b, b1, b2> := FreeGroup (5);
Alpha := [a1, a2, b];
Beta := [b1, b2];

// common relations, typically commutators
Comms := [
(a1, a2) = b, (b, a1)= b1, (b, a2) = b2, (b1, b2) = 1,
b^p = 1, b1^p = 1, b2^p = 1] cat
&cat([[(Beta[i], Alpha[j]) = Id(F) : j
 in [1..3]]: i in [1..2]]);

L := [];

// (221)a

G := quo <F | Comms, a1^p = b1, a2^p = b2>;
Append (~L, G);

// (221)b_r

for r in [1..(p-1) div 2] do
  G := quo <F | Comms, a1^p = b1^(w^r mod p), a2^p = b2>;
  Append (~L, G);
end for;

// (221)c_r

for r in [1, nu] do
  G := quo <F | Comms, a1^(4*p) = b2^(-r), a2^p = b1^r*b2^r>;
  Append (~L, G);
end for;

// (221)d_0

G := quo <F | Comms, a1^p = b2, a2^p = b1^nu>;
Append (~L, G);

// (221)d_r

for r in [1..(p-1) div 2] do
  x := PairPhi6 (r, p);
  G := quo <F | Comms, a1^p = b2^x, a2^p = b1*b2>;
  Append (~L, G);
end for;

// (2111)a

G := quo <F | Comms, a1^p = b1, a2^p = 1>;
Append (~L, G);

// (2111)b_r

for r in [1, nu] do
G := quo <F | Comms, a1^p = 1, a2^p = b1^r>;
Append (~L, G);
end for;

// (1^5)

G := quo <F | Comms, a1^p = 1, a2^p = 1>;
Append (~L, G);

return L;
end function;


My7 := function(p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

F<a, a1, a2, a3, b> := FreeGroup (5);
Alpha := [a, a1, a2, b];
Beta := [a3];

// common relations, typically commutators
Comms := [
(a1, a) = a2, (a2, a) = a3, (b, a) = 1,
(a1, a2) = 1, (a1, b)= a3, (a2, b) = 1, 
a2^p = 1, a3^p = 1] cat
&cat([[(Beta[i], Alpha[j]) = Id(F) : j
 in [1..4]]: i in [1]]);

L := [];


// (2111)a

G := quo <F | Comms, a^p = a3, a1^p = 1, b^p = 1>;
Append (~L, G);

// (2111)b_r

for r in [1, nu] do
  G := quo <F | Comms, a^p = 1, b^p = 1, a1^p = a3^r>;
  Append (~L, G);
end for;

// (2111)c

G := quo <F | Comms, a^p = 1, a1^p = 1, b^p = a3>;
Append (~L, G);

// (1^5)

G := quo <F | Comms, a^p = 1, a1^p = 1, b^p = 1>;
Append (~L, G);

return L;
end function;


My8 := function(p)

F<a1, a2, b> := FreeGroup (3);
Alpha := [a1, a2]; Beta := [b];

// common relations, typically commutators
Comms := [
(a1, a2) = b, (b, a2) = b^p, b^(p^2) = 1];

L := [];

// (32)a

G := quo <F | Comms, a1^p = b, a2^(p^2) = 1>;
Append (~L, G);

return L;
end function;


My9 := function(p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

F<a, a1, a2, a3, a4> := FreeGroup (5);
Alpha := [a, a1, a2, a3];
Beta := [a4];

// common relations, typically commutators
Comms := [
(a1, a) = a2, (a2, a) = a3, (a3, a) = a4,
(a1, a2) = 1, (a1, a3)= 1, (a2, a3) = 1, 
a2^p = 1, a3^p = 1, a4^p = 1] cat
&cat([[(Beta[i], Alpha[j]) = Id(F) : j
 in [1..4]]: i in [1]]);

L := [];


// (2111)a

G := quo <F | Comms, a^p = a4, a1^p = 1>;
Append (~L, G);

// (2111)b_r

el3 := GCD(p-1, 3) - 1;
for r in [0..el3] do
  G := quo <F | Comms, a^p = 1, a1^p = a4^(w^r mod p)>;
  Append (~L, G);
end for;

// (1^5)

G := quo <F | Comms, a^p = 1, a1^p = 1>;
Append (~L, G);

return L;
end function;


My10 := function(p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

F<a, a1, a2, a3, a4> := FreeGroup (5);
Alpha := [a, a1, a2, a3];
Beta := [a4];

// common relations, typically commutators
Comms := [
(a1, a) = a2, (a2, a) = a3, (a3, a) = a4,
(a1, a2) = a4, (a1, a3)= 1, (a2, a3) = 1, 
a2^p = 1, a3^p = 1, a4^p = 1] cat
&cat([[(Beta[i], Alpha[j]) = Id(F) : j
 in [1..4]]: i in [1]]);

L := [];


// (2111)a_r

el4 := GCD(p-1, 4) - 1;
for r in [0..el4] do
  G := quo <F | Comms, a^p = a4^(w^r mod p), a1^p = 1>;
  Append (~L, G);
end for;

// (2111)b_r

el3 := GCD(p-1, 3) - 1;
for r in [0..el3] do
  G := quo <F | Comms, a^p = 1, a1^p = a4^(w^r mod p)>;
  Append (~L, G);
end for;

// (1^5)

G := quo <F | Comms, a^p = 1, a1^p = 1>;
Append (~L, G);

return L;
end function;


Table3 := function(p)
return My3(p) cat My4(p) cat My6(p) cat My7(p) cat
 My8(p) cat My9(p) cat My10(p);
end function;

CheckTable3 := function(L, p)

K := GF(p);

w := PrimitiveRoot(p);

NonQuadraticResidue := function (p)
  for x in GF (p) do
    if not IsPower (x, 2) then return x; end if;
  end for;
end function;

Z := Integers ();
    nu := Z ! (NonQuadraticResidue (p)); // nu

count := 0;

Plist := []; // lists the group G from Table 3
codG := [];  // codG from Table 3

// phi_3(311)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^4]);

// phi_3(311)b_r

for r in [1, nu] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^4]);
end for;

// phi_3(221)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_3(221)b_r

for r in [1, nu] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_3(2111)d

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_3(2111)e

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_3(2111)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_3(2111)b_r

for r in [1, nu] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_3(1^5)

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_3(311)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^4]);

// phi_4(221)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_4(221)b

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_4(221)c

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2]);

// phi_4(221)d_r

for r in [1..(p-1) div 2] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_4(221)e

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_4(221)f_0

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^3]);

// phi_4(221)f_r

for r in [1..(p-1) div 2] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^3]);
end for;

// phi_4(2111)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2]);

// phi_4(2111)b

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_4(2111)c

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_4(1^5)

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2]);

// phi_6(221)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_6(221)b_r

for r in [1..(p-1) div 2] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_6(221)c_r

for r in [1, nu] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_6(221)d_0

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_6(221)d_r

for r in [1..(p-1) div 2] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_6(2111)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_6(2111)b_r

for r in [1, nu] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_6(1^5)

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_7(2111)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_7(2111)b_r

for r in [1, nu] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_7(2111)c

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_7(1^5)

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_8(32)

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

// phi_9(2111)a

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3, p^4]);

// phi_9(2111)b_r

el3 := GCD(p-1, 3) - 1;
for r in [0..el3] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3, p^4]);
end for;

// phi_9(1^5)

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3, p^4]);

// phi_10(2111)a_r

el4 := GCD(p-1, 4) - 1;
for r in [0..el4] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_10(2111)b_r

el3 := GCD(p-1, 3) - 1;
for r in [0..el3] do
count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);
end for;

// phi_10(1^5)

count := count+1;
F := L[count];
GP, phi := pQuotient(F, p, 5);
Append(~Plist, GP);
Append(~codG, [1, p, p^2, p^3]);

return Plist, codG;
end function;

for p in PrimesInInterval (5, 13) do 
   "\nprocess p = ", p;
   L := Table3(p);

   Plist, codG := CheckTable3(L, p);
   
for i in [1..#L] do
      P := Plist[i];
      codG_input := codG[i];
      CodGP := CharacterCodegree(P);
      assert CodGP eq codG_input;

      end for;
end for;

